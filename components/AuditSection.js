import styles from "../styles/AuditSection.module.css";

export default function AuditSection() {
  return (
    <section className={styles.section}>
      <h2>audit section</h2>
    </section>
  );
}
